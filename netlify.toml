[build]
  publish = "public"
  command = "mkdir -p public && echo 'Deployed' > public/index.html"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
