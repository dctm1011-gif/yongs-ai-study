Connect ADB and install latest YongStudy debug APK. Port changes on reboot.

```bash
adb connect 192.168.219.135:42255
adb install -r /c/Users/dctm1/YongStudyApp/android/app/build/outputs/apk/debug/app-debug.apk
```
